<UserControl x:Class="WpfAppUI.Controls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfAppUI.Controls"
             xmlns:nav="clr-namespace:WpfAppUI.State.Navigators"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="{StaticResource BrushPrimary1}">
            <TextBlock Text="Stok Takip" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,5" />
        </Grid>

        <Grid Grid.Row="1" Background="{StaticResource BrushPrimary2}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Anasayfa"
                    Background="{StaticResource ButtonBrushPrimary}"
                    FontSize="12"
                    Width="80" 
                    Height="20" 
                    Margin="5" 
                    Grid.Column="0" 
                    Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static nav:ViewType.Home}" />

            <Button Content="Ürünler" 
                    Background="{StaticResource ButtonBrushPrimary}"
                    FontSize="12"
                    Width="80" 
                    Height="20" 
                    Margin="5" 
                    Grid.Column="1" 
                    Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static nav:ViewType.Products}"/>

        </Grid>
    </Grid>
</UserControl>
